// import * as cdk from "@aws-cdk/core"

test("Empty Stack", () => {
  // const app = new cdk.App()
  /*
  // WHEN
  new SomeStack(app, "MyTestStack", {

  })
  // THEN
  // TODO: Find some logical test units we can cover, that does not
  //  overlap too much with our snapshot tests.
  expectCDK(stack).to(
    matchTemplate(
      {
        Resources: {},
      },
      MatchStyle.EXACT,
    ),
  )
  */
})
